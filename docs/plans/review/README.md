# Plans Awaiting Review

This directory holds plans whose delivery pull request is open. A plan enters `review` in that same PR and records its PR URL, delivery commit, validation, and final version decision. It moves to `completed` only after the PR is merged and local `main` is synchronized. A review plan remains ahead of every later Roadmap item until it is reconciled.
